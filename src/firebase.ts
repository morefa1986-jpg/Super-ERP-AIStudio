/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
 */

// Cloud Firebase is intentionally disabled. The ERP is local-first and must
// remain fully functional on an isolated farm network without Internet access.
export const app = null;
export const db = null;
export const auth = null;
